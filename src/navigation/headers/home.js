import * as React from 'react';
import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

export const HomeHeaderBar = ({name, size, color}) => {
  return <Icon name={name} size={size} color={color} />;
};
